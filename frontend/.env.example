# Backend API base URL.
# Leave unset in dev to use the default http://127.0.0.1:3000.
# In production, set this to your real API origin (e.g. https://api.example.com).
VITE_API_BASE=http://127.0.0.1:3000
